

const capitalizedNames= (nameArr) => {
  const newArr = nameArr.map ((name)=> name.upperCase() + nameArr.slice(1));  return newArr;
}


const removeVowels =(vowel) => {
  const wordWithoutVowels = vowel.filter
}



const removeFromArr = (string, array) => {
    const filteredArr = stringArr.filter(newstring => newstring != string)
    return filteredArr
}